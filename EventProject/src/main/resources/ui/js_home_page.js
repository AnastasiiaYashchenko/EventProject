let linkElement;let butn_reservation;fetch('http://localhost:8080/Event/all')    .then(response => response.json())    .then(datas => {        const eventsDiv = document.getElementById('events_Block');        const eventsContainer = document.getElementById('eventsContainer');        datas.forEach(event => {            const eventContainer = eventsDiv.cloneNode(true);            eventContainer.style.display = 'block';            linkElement = eventContainer.querySelector("a");            linkElement.href = `one_event.html?id=${event.id}`;            butn_reservation = eventContainer.querySelector('div.buttn_center>a');            butn_reservation.href = `one_event.html?id=${event.id}`;            const imgTag = eventContainer.querySelector('div.div_main_img>img');            imgTag.setAttribute('src', event.image);            eventContainer.querySelector('h4').textContent = event.nameEvent;            eventContainer.querySelector('div.img_date_EB > div.cl_date').textContent = event.dayEvent;            eventContainer.querySelector('div.img_time_EB > div.cl_time').textContent = event.timeEvent;            eventContainer.querySelector('div.img_address_EB>div.cl_address').textContent = event.address;            eventContainer.querySelector('div.img_price_EB>div.cl_price').textContent = event.price;            // Добавляем контейнер в общий контейнер на странице            if (eventsContainer.firstChild) {                eventsContainer.insertBefore(eventContainer, eventsContainer.firstChild);            } else {                eventsContainer.appendChild(eventContainer);            }            console.log(datas);        })    })    .catch(error => {        console.error('Error fetching data:', error);    });